spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Anky2404/config-repo.git
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"
